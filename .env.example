# Build-time key for Gemini image generation.
# For local dev, create `.env.local` (gitignored) and set this value.
VITE_GEMINI_API_KEY=

# Gemini API key for AI favicon generation (injected at build time; never shown in the UI).
# Copy to `.env.local` for local development.
VITE_GEMINI_API_KEY=
